#include <iostream>
#include <boost/align.hpp>

int main() {

	std::cout << "This is just a test to get the solution started and have the boost library set up so we all dont have to mess around with it" << std::endl;

	system("pause");
}